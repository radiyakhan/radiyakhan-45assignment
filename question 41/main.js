// Define a function to print each magictian name from an array  
function show_magicians(magictians) {
    magictians.forEach(function (name) { return console.log(name); });
}
// Define an array containing magicians name
var magician_names = ["Harry potar", "Radiya", "Subhan"];
// call the function to print each magician name
show_magicians(magician_names);
